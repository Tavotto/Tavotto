# Flask 父进程依赖（.venv）的钉死镜像——与 pyproject 的 `dependencies` 同名同序
# （tests/test_rendercore_fonts.py 看护）。注意：这里刻意没有 matplotlib——
# engine/registry.py、pool.py、ai_bridge.py 必须保持纯标准库可 import，
# 科学栈由 worker 解释器提供（pool.find_worker_python / TAVOTTO_WORKER_PYTHON）。
# RenderCore 自 U10 起是默认后端（ADR 0072）：五个包的版本与 ADR 0055 §1 实测钉死的相同；
# PyMuPDF 已退役，不在这里（测试的读取器走 `pip install -e ".[dev]"` 的 legacy-pymupdf extra）。
flask==3.1.3
packaging==26.3
pikepdf==10.13.0.post1
fonttools==4.65.0
uharfbuzz==0.56.1
pypdfium2==5.13.0
pillow==12.3.0
