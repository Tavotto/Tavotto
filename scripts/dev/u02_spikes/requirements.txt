# U02 render_spike 报告（docs/implementation/tavotto-foundation/evidence/u02/render/report.json）记的候选包版本——
# **只是证据记录**：tests/test_foundation_u02_render.py 读它对拍报告里的版本号。spike 代码已退役
# （render 半边由 U06 / U07 收编，runtime 半边由 U05 收编、随 chore/retire-u02-runtime-spike 删除），
# 这里不再是任何 venv 的安装清单；ADR 0055 / 0056 与 evidence/u02/ 留作记录。
# 写入侧
pikepdf==10.13.0.post1
fonttools==4.65.0
uharfbuzz==0.56.1
# 栅格 / 读取侧（三把尺子）
pypdfium2==5.13.0
pdfminer.six==20251107
pypdf==6.16.1
